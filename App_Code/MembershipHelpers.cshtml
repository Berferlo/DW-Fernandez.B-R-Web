@helper AccountSummary()
{
    <div id="registro">
        @if(WebSecurity.IsAuthenticated){
            
            <p>
                Bienvenido <a href="@Href("~/changePassword")">@WebSecurity.CurrentUserName</a> |
                <a href="@Href("~/logout")"> Cerrar Sesion</a>
        
            </p>

        }
        else
        {
            <p>
                <a href="@Href("~/login")">Iniciar sesion</a> |
                <a href="@Href("~/Registrar")">Registrarse</a>
            </p>
        }
    </div>
}

@helper existeUsuarioEncuesta()
{
    var db = Database.Open("Dawemos");
    var sqlCommand = "SELECT * FROM Votaciones WHERE userName="+usu+ " AND numEncuesta="+id;
    var result = db.Query(sqlCommand);

    
}
